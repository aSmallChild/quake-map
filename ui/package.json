{
  "name": "ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages": "npx wrangler pages dev ./dist",
    "pages-do": "wrangler pages dev dist -o GEONET=Geonet",
    "pages-dev": "npx wrangler pages dev dist -- npx vite"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.0-next.30",
    "svelte": "^3.44.0",
    "vite": "^2.8.0",
    "wrangler": "^0.0.17"
  }
}
